// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://chaitueditzdigitalmedia.com',
  base: '/new',
  output: 'static',
});
